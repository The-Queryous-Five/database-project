
DB_HOST=localhost
DB_PORT=3306
DB_NAME=olist


# Flask Configuration
FLASK_ENV=development
FLASK_APP=app/app.py

# Development Workflow:
# 1. Copy this file to .env in the project root
# 2. Set DB_VENDOR to either 'postgres' or 'mysql'
# 3. Update the corresponding DB_* variables with your local database credentials
# 4. Make sure your database server is running
# 5. Apply DDL scripts from db/ddl/ (for Postgres) or db/ddl_mysql/ (for MySQL)
# 6. Run ETL scripts to load data (db/etl/)
# 7. Start the Flask app: flask run
