# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Choose your database vendor: 'postgres' or 'mysql'
DB_VENDOR=postgres

# -----------------------------------------------------------------------------
# PostgreSQL Configuration (used when DB_VENDOR=postgres)
# -----------------------------------------------------------------------------
# Host where PostgreSQL server is running (usually 'localhost' for local dev)
DB_HOST=localhost
# PostgreSQL default port
DB_PORT=5432
# Name of your PostgreSQL database
DB_NAME=olist
# PostgreSQL username
DB_USER=postgres
# PostgreSQL password - CHANGE THIS to your actual password!
DB_PASS=changeme

# -----------------------------------------------------------------------------
# MySQL Configuration (used when DB_VENDOR=mysql)
# -----------------------------------------------------------------------------
# If you want to use MySQL instead, uncomment the following lines and 
# comment out the PostgreSQL settings above:
#
# DB_VENDOR=mysql
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=olist
# DB_USER=root
# DB_PASS=changeme

# =============================================================================
# FLASK CONFIGURATION
# =============================================================================
FLASK_ENV=development
FLASK_APP=app/app.py

# =============================================================================
# DEVELOPMENT WORKFLOW
# =============================================================================
# 1. Copy this file to .env in the project root:
#    cp .env.example .env
#
# 2. Edit .env and set DB_VENDOR to either 'postgres' or 'mysql'
#
# 3. Update the corresponding DB_* variables with your REAL credentials:
#    - DB_HOST (usually localhost for local development)
#    - DB_PORT (5432 for Postgres, 3306 for MySQL)
#    - DB_NAME (the database name, e.g., 'olist')
#    - DB_USER (your database username)
#    - DB_PASS (your database password - DO NOT use 'changeme' in production!)
#
# 4. Make sure your database server is installed and running:
#    - PostgreSQL: Check with `psql --version` and start service
#    - MySQL: Check with `mysql --version` and start service
#
# 5. Create the database if it doesn't exist:
#    - Postgres: psql -U postgres -c "CREATE DATABASE olist;"
#    - MySQL: mysql -u root -p -e "CREATE DATABASE olist;"
#
# 6. Apply DDL scripts to create tables:
#    - For PostgreSQL: Run scripts in db/ddl/ directory
#    - For MySQL: Run scripts in db/ddl_mysql/ directory
#
# 7. Run ETL scripts to load data:
#    - Execute scripts in db/etl/ directory to populate tables
#
# 8. (Optional) Test database connection before running the app:
#    python -m tools.test_db_connection
#
# 9. Start the Flask application:
#    flask run
#
# 10. Access the application at http://127.0.0.1:5000
#
# =============================================================================
# TROUBLESHOOTING
# =============================================================================
# If you see connection errors:
# - Verify your DB server is running
# - Check credentials in .env match your database
# - Run: python -m tools.test_db_connection for detailed diagnostics
# - Check logs for specific error messages (host, port, authentication, etc.)
